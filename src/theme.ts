/**
 * Centralized app theme — single source of truth for colors, spacing, radii,
 * and standardized typography across every screen and component.
 */

export * from './theme/index';
export { default } from './theme/index';
